# RouteWise backend Python dependencies.
# Minimal, install-script-driven; pinned loosely to mirror RigSense.

# --- Embeddings (offline MiniLM) ---
# 5.x is required for the model files in models/all-MiniLM-L6-v2 (modules.json
# references sentence_transformers.base.modules.transformer.Transformer).
sentence-transformers>=5.0,<6
torch>=2.2

# --- Vector DB client (installed from local wheel by install script) ---
# actian-vectorai==0.1.0b2  # see vectorai-db-beta/actian_vectorai-0.1.0b2-py3-none-any.whl

# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.4

# --- Geo / clustering / exposure ---
h3>=4.0
shapely>=2.0
pyproj>=3.6
geopandas>=1.0       # required by backend/services/segments.py for AADT spatial join
pyogrio>=0.9         # geopandas IO backend (faster than fiona, ships wheels for win)
scikit-learn>=1.4
numpy>=1.26
pandas>=2.1

# --- Sun position ---
pysolar>=0.11

# --- Storage ---
pyarrow>=15.0

# --- Dev / convenience ---
python-dotenv>=1.0
pytest>=8.0
